import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: { marginVertical: 12, alignItems: 'center' },
  level: { fontSize: 18, fontWeight: 'bold', color: '#fff' },
  bar: { width: 120, height: 10, backgroundColor: '#333', borderRadius: 5, marginVertical: 6 },
  fill: { height: 10, backgroundColor: '#4caf50', borderRadius: 5 },
  streak: { fontSize: 14, color: '#fff' },
});