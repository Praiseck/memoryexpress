import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  stats: {
    backgroundColor: '#2a2a2a',
    padding: 15,
    borderRadius: 10,
    marginBottom: 0,
  },
  label: {
    color: '#fff',
    fontSize: 16,
    marginBottom: 0,
  },
});